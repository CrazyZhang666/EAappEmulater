<UserControl
    x:Class="EAappEmulater.Views.DownloadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://wpf.microsoft.com/ui/2024/xaml"
    d:DesignHeight="600"
    d:DesignWidth="1000"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox
            x:Name="ListBox_Games"
            Margin="5"
            ItemsSource="{Binding Games}"
            SelectedItem="{Binding SelectedGame, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="2">
                        <TextBlock
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Right"
                            FontSize="10"
                            Foreground="Gray"
                            Text="{Binding GameType}" />
                        <StackPanel Margin="5,0,5,0" VerticalAlignment="Center">
                            <TextBlock
                                FontSize="14"
                                FontWeight="Bold"
                                Text="{Binding Name}" />
                            <TextBlock
                                Margin="0,3,0,0"
                                FontSize="10"
                                Foreground="Gray"
                                Text="{Binding OfferId}" />
                        </StackPanel>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <StackPanel Margin="5,0,0,0" VerticalAlignment="Center">
                    <TextBlock
                        d:Text="Game Name"
                        FontSize="15"
                        FontWeight="Bold"
                        Text="{Binding SelectedGame.Name}" />
                    <TextBlock
                        Margin="0,5,0,0"
                        d:Text="OfferId"
                        Foreground="Gray"
                        Text="{Binding SelectedGame.OfferId}" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <ui:IconHeader Title="GameName" DockPanel.Dock="Top" />
                <ui:TextBoxHint
                    IsReadOnly="True"
                    Text="{Binding SelectedGame.Name}" />

                <ui:IconHeader Title="OfferId" DockPanel.Dock="Top" />
                <ui:TextBoxHint
                    Hint="OfferId"
                    IsReadOnly="True"
                    Text="{Binding SelectedGame.OfferId}" />

                <ui:IconHeader Title="GameType" DockPanel.Dock="Top" />
                <ui:TextBoxHint
                    IsReadOnly="True"
                    Text="{Binding SelectedGame.GameType}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,10,0,0">
                <ui:IconHeader Title="DownloadSize" DockPanel.Dock="Top" />
                <ui:TextBoxHint
                    IsReadOnly="True"
                    Text="{Binding DownloadSize}" />

                <ui:IconHeader Title="DownloadUrl" DockPanel.Dock="Top" />
                <TextBox
                    Margin="0,5,0,0"
                    Text="{Binding DownloadUrl}"
                    IsReadOnly="True"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    Height="140"
                    VerticalScrollBarVisibility="Auto" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
