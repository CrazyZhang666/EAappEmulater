<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ModernWpf.Controls">
    <Style TargetType="{x:Type controls:OwnedGameMenu}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:OwnedGameMenu}">
                    <Grid Width="300" Height="72" Margin="5">
                        <Border Background="White" CornerRadius="4" SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="5" Direction="315" Opacity="0.5" ShadowDepth="0" Color="Black" />
                            </Border.Effect>
                        </Border>
                        <Grid Margin="6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="16" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{TemplateBinding GameName}" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                            <Grid Grid.Row="1">
                                <TextBlock Text="{TemplateBinding OfferId}" FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Left" Foreground="#666" />
                                <TextBlock Text="{TemplateBinding GameTypeText}" FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="#666" />
                            </Grid>
                        </Grid>
                        <Grid x:Name="MaskGrid" Visibility="Hidden">
                            <Border Background="Black" Opacity="0.45" CornerRadius="4" />
                            <Button x:Name="PART_ButtonDownload" Width="90" Height="33" Content="Download" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MaskGrid" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>